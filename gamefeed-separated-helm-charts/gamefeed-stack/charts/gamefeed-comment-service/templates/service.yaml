apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamefeed-comment-service.resourceName" . }}
  namespace: {{ include "gamefeed-comment-service.namespace" . }}
  labels:
    app: {{ include "gamefeed-comment-service.resourceName" . }}
    {{- include "gamefeed-comment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "gamefeed-comment-service.resourceName" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      {{ if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{ end }}
