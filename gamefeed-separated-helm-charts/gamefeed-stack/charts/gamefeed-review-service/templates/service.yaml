apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamefeed-review-service.resourceName" . }}
  namespace: {{ include "gamefeed-review-service.namespace" . }}
  labels:
    app: {{ include "gamefeed-review-service.resourceName" . }}
    {{- include "gamefeed-review-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "gamefeed-review-service.resourceName" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      {{ if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{ end }}
